/**
 * PostCSS Configuration
 *
 * WHY we use these plugins:
 * - tailwindcss: Processes Tailwind directives and generates utility classes
 * - autoprefixer: Automatically adds vendor prefixes for cross-browser compatibility
 */
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

